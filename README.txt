This  codes are divided in to 3 files IT_4_BDAS final work is main file Iteration 2 and Iteration 3 are 2 different 
Interation of main model.
Note: Code is sucessefully running on local machine and AWS instance jupyter Notebook.

If code not run in to Ubuntu AWS use Anaconda with following specifications.

To install spark on local machine with Anaconda.
Step1> open conda terminal.
Step2>To install this package with conda run one of the following:
>conda install -c conda-forge spacy
>conda install -c conda-forge/label/gcc7 spacy
>conda install -c conda-forge/label/cf201901 spacy
>conda install -c conda-forge/label/cf202003 spacy
>open jupyter notebook
>import code files
>Run


>to Install seaborn:
>sudo apt-get install -y python3-dev python3-setuptools
>sudo apt-get update
>sudo apt-get install python3-pip
>pip3 install -U pip
>pip install seaborn --user
